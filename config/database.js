// config/database.js

// This is the file used for config, to be modified to point to the MongoDB database to use
module.exports = {
    // Reference the mongodb instance, can be local or remote
    'url' : 'mongodb://localhost/contest_scoreboard'
};
